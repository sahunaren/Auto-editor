name: video_editor_example
description: The example project for the video_editor package.
publish_to: "none"
version: 3.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  video_editor: ^3.0.0
  fraction: ^4.1.4
  image_picker: ^0.8.9
  path: ^1.8.2
  video_player: ^2.6.1
  ffmpeg_kit_flutter_new: ^6.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
